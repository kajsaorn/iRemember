<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.iremember.subscriber.iremembersubscriber.ReminderActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:padding="@dimen/layout_padding"
        android:background="@android:color/holo_purple">

        <TextView
            android:id="@+id/tv_reminder_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_orange_light"
            android:textColor="@android:color/background_light"
            android:textSize="@dimen/tv_textsize_large"
            android:textAlignment="center"
            android:textStyle="bold"
            />

        <Button
            android:id="@+id/btn_reminder_stop"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/holo_green_light"
            android:padding="@dimen/btn_padding_large"
            android:text="@string/btn_reminder_stop"
            android:textColor="@android:color/background_light"
            android:textSize="@dimen/btn_textsize_large"
            android:textStyle="bold"
            android:onClick="onStopClick"
            android:layout_below="@id/tv_reminder_label"
            />

    </RelativeLayout>
</FrameLayout>
